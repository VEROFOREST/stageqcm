 {{ form_start(form) }}
    <h3>Questionnaire</h3>
    <div class="row">
        <div class="col-md-12">
        {{ form_row(form.questionnaire) }}
        </div>
    </div>
    
    
    <h3>Questions</h3>
  
    <div class="row questionwrapper" data-prototype="{{ form_widget(form.question.vars.prototype)|e     ('html_attr') }}" data-index="{{ form.question|length }}">
      
        {% for quest in form.question %}
            {% block questions %} 
                <div class="form-group col-md-12">
                      <h6 > Question</h6>
                    {# {{ form_row(quest) }} #}
                     {{form_label(quest.numero, 'N°', { 'attr': {'class': 'foo'} } )}} 
                    {{ form_widget(quest.numero,{'attr': {'class': "form-control",}} )}}
                    {{ form_label(quest.libelle,'Libellé de la question', { 'attr': {'class': 'foo'} } )}} 
                    {{ form_widget(quest.libelle,{'attr': {'class': "form-control",}} )}}
                    {{ form_label(quest.baremeQuestion,'Barème de la question', { 'attr': {'class': 'foo'}} )}} 
                    {{ form_widget(quest.baremeQuestion,{'attr': {'class': "form-control",}})}}
                    {{ form_label(quest.typeReponse,'Est ce QCM ou réponse libre ?', { 'attr': {'class': 'foo'}})}} 
                    {{ form_widget(quest.typeReponse,{'attr': {'class': "form-control",}})}}
                    {# {% if  form.number.vars.value == 1 %} %}
                        
                    {% endif %} #}
                    {{ form_label(quest.nbreChoix,'si QCM, nombre de choix possible',{ 'attr': {'class': 'foo'}})}} 
                    {{ form_widget(quest.nbreChoix,{'attr': {'class': "form-control",}})}}
             {% endblock %}
            {% block reponses %} 
                    <h3>Réponse(s)</h3> 
               {# <div class="row reponsewrapper" data-prototype="{{ form_widget(form.reponseProf.vars.prototype)|e     ('html_attr') }}" data-index="{{ form.reponseProf|length }}"> #} #}
            
            
                    {% for rep in form.reponseProf %}
                        <div class=" form-group col-md-12">
                            {{ form_row(rep) }}
                        </div>
                    {% endfor %}
             {# </div> #}
                
           
       
       
                    
                 </div>
                
            {% endblock %}
         
        {% endfor %}
        
      </div>
      
 
       
        {# <a href="#" class="jsaddquestions">
        <span class="fa fa-close"> +++</span>
        </a> #}
  
    
     <button class="btn">{{ button_label|default('Save') }}</button>
     
{{ form_end(form) }}
 
 
